import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["mealsuite-menu-images-prod.s3.ap-south-1.amazonaws.com"],
  },
};

export default nextConfig;
